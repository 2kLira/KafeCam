# Xcode build artifacts
DerivedData/
build/

# User data — personal Xcode IDE files, never commit
*.xcuserstate
xcuserdata/
*.xcuserdatad/

# SwiftPM working dirs (keep resolved file tracked)
.swiftpm/
Packages/

# macOS
.DS_Store

# Supabase secrets — never commit
KafeCamSecrets.xcconfig
