# Xcode build artifacts
DerivedData/
build/

# User data
*.xcuserstate
*.xcuserdata/

# SwiftPM working dirs (keep resolved file tracked)
.swiftpm/
Packages/

# macOS
.DS_Store

# Supabase secrets — never commit
# KafeCamSecrets.xcconfig contains real API keys — never commit it
KafeCamSecrets.xcconfig
KafeCam/Networking/SupabaseConfig.swift
